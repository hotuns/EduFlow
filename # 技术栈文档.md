# 技术栈文档

## 概述

无人机教学系统是一个基于 Electron 的桌面端教学考核平台，集本地视频学习、题库练习、模拟考试、自动评分与证书导出为一体。渲染层采用 Vue 3 + Naive UI + UnoCSS，数据持久化使用 electron-store，题库从本地 Excel 加载，视频资源从本地 `data/videos` 读取。

## 技术总览

- 桌面与构建：Electron、Vite、electron-builder
- 前端框架：Vue 3、TypeScript、Pinia、vue-router
- UI 与样式：Naive UI、UnoCSS、Iconify Carbon
- 数据与持久化：electron-store、xlsx（Excel 解析）
- 导出与文档：html2canvas、jsPDF
- 自动引入：unplugin-auto-import、unplugin-vue-components（NaiveUiResolver）

## 目录结构

- `src/`
  - `views/`：页面（`Home.vue`、`Learn.vue`、`Exam.vue`、`Admin.vue`、`User.vue`）
  - `components/`：组件（`Videos.vue`、`Questions.vue`、`APPExam.vue`、`Login.vue`、`Result.vue`）
  - `store/`：状态与持久化（Pinia + electron-store）
  - `datas.ts`：数据管理与题库转换
  - `route.ts`：路由与权限守卫
- `electron/`：主进程与预加载（`main.ts`、`preload.ts`）
- `data/`：本地数据目录（打包复制到 `resources/data`）
- 构建配置：`vite.config.ts`、`electron-builder.json5`

## 前端框架

- Vue 3 组合式 API + TypeScript，静态类型检查使用 `vue-tsc`
- 路由与权限：未登录访问除首页外页面会重定向到首页，非管理员禁止访问 `admin`
  - 参考：`src/route.ts:37–53`
- 状态与持久化：用户、分值、抽题数量、折叠状态等通过 `watch` 自动写入 electron-store
  - 参考：`src/store/index.ts:219–257`

## 桌面与构建

- Vite 插件：
  - `@vitejs/plugin-vue`：Vue 构建
  - `vite-plugin-electron/simple`：主进程与预加载打包
  - `vite-plugin-electron-renderer`：渲染层 Node/Electron API 适配
  - 参考：`vite.config.ts:12–55`
- Electron 主进程：
  - 窗口、菜单、数据目录、IPC、Excel 解析
  - 渲染层启用 `nodeIntegration`，关闭 `contextIsolation`，并通过 `preload.ts` 暴露 `ipcRenderer`
  - 参考：`electron/main.ts:144–170, 48–72, 74–142`

## 数据与持久化

- 渲染层持久化：`electron-store`
  - 初始化管理员账号、保存用户考试记录与视频学习进度
  - 参考：`src/store/index.ts:58–72, 182–200`
- 数据目录：
  - 开发：`APP_ROOT/data`（`electron/main.ts:37–46`）
  - 打包：`resources/data`（`electron-builder.json5:35–40`）

## UI 与样式

- Naive UI：现代化组件库，搭配自动引入与解析器
- UnoCSS：原子化样式与重置（`@unocss/reset`）
- 图标：`@iconify-json/carbon`
- 引入与解析：
  - 参考：`vite.config.ts:15–31`

## 题库与考试

- Excel 读取：主进程用 `xlsx` 从 `data/*.xls` 解析
  - 参考：`electron/main.ts:74–142`
- 题型：单选、判断、多选、填空、简答、扩展
  - 定义与转换：`src/datas.ts:17–51, 190–298`
- 抽题与分值：根据管理员设置随机抽题并赋分
  - 参考：`src/datas.ts:53–83`
- 简答题评分：关键词匹配比例 × 分值（四舍五入）
  - 参考：`src/components/APPExam.vue:756–796`

## 证书与导出

- 证书导出：`html2canvas`（转图片）+ `jsPDF`（生成 PDF）
  - 参考：`src/components/Result.vue:142–196`
- 通过判定：
  - 以“考试当时记录的 `passScore`”为准，未记录时回退当前设置计算（默认 60%）
  - 参考：`src/components/Result.vue:115–122`
- 显示样式：
  - 成绩颜色：通过为绿色，不通过为红色
  - 参考：`src/components/Result.vue:52–55`

## 路由与权限

- 路由表：`src/route.ts:4–34`
- 守卫：
  - 未登录禁止访问非首页
  - 非管理员禁止访问后台
  - 参考：`src/route.ts:37–53`
- 考试页的离开确认与刷新保护
  - 参考：`src/components/APPExam.vue:1049–1104`

## 打包配置

- Windows：NSIS（x64/ia32），产物命名 `${productName}-Windows-${version}-Setup.${ext}`
  - 参考：`electron-builder.json5:15–23, 24–29`
- macOS：DMG（`electron-builder.json5:11–14`）
- Linux：AppImage、deb（`electron-builder.json5:30–34`）
- 复制资源：`extraResources -> data`（`electron-builder.json5:35–40`）

## 脚本与运行

- 开发：`pnpm dev` 或 `npm run dev`
- 构建：`pnpm build` 或 `npm run build`
- Linux 打包：`pnpm build:linux` 或 `npm run build:linux`
- 主入口：`package.json:37`

## 关键文件

- IPC 与 Excel 解析：`electron/main.ts:48–72, 74–142`
- 数据管理与转换：`src/datas.ts:105–167, 190–298`
- 抽题与评分：`src/components/APPExam.vue:670–754, 809–885, 909–926`
- 证书生成与导出：`src/components/Result.vue:142–196`
- 状态与持久化：`src/store/index.ts:74–218, 219–257`

## 数据流与通信

- 渲染层通过 IPC 调用主进程读取数据与校验文件（`electron/main.ts:48–72`）
- `DataManager` 统一加载与转换数据，并提供题库与视频数据（`src/datas.ts:105–167, 300–327`）
- 考试提交后写入 `electron-store` 并同步当前用户视图（`src/store/index.ts:182–200`）

## 安全与注意事项

- 渲染层启用 `nodeIntegration` 与禁用 `contextIsolation` 仅用于本地数据场景，避免网络环境下的风险
- 主进程限制访问范围为 `data` 目录，通过受控 IPC 操作文件
- 不在仓库或日志中记录敏感信息，确保持久化内容最小化

## 版本与依赖

- 核心依赖：`vue@^3.4`、`pinia@^2.3`、`vue-router@4`、`naive-ui@^2.40`、`unocss@^0.65`、`electron@^30`、`electron-builder@^24`、`vite@^5.1`
- 数据与导出：`xlsx@^0.18`、`html2canvas@^1.4`、`jspdf@^2.5`
- 自动引入：`unplugin-auto-import`、`unplugin-vue-components`（含 NaiveUiResolver）
